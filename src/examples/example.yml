description: >
# Provide a use-case based example for using this orb.
# Everything in the `usage` section will be displayed in the orb registry.
# Comments are not retained.
usage:
  version: 2.1
  orbs:
    workflow: rhems-japan/workflow@x.y.z
  jobs:
    your-job:
      machine: true
      steps:
        - rhems-japan/call-workflow:
            org-or-user: RHEMS-Japan
            repo: orbs-workflow
  workflows:
    your-workflow:
      jobs:
        - your-job
